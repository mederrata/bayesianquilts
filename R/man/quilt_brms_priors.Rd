% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/brms_quilt.R
\name{quilt_brms_priors}
\alias{quilt_brms_priors}
\title{Build brms priors from a Decomposed quilt structure}
\usage{
quilt_brms_priors(
  decomposed,
  scales,
  interaction_sep = ":",
  intercept_scale = NULL
)
}
\arguments{
\item{decomposed}{A \code{Decomposed} object.}

\item{scales}{A named numeric vector / list of prior scales (typically
produced by \code{decomposed$generalization_preserving_scales(...)}). Names
must match the keys in \code{decomposed$.tensor_part_interactions}.}

\item{interaction_sep}{See \code{quilt_brms_formula}.}

\item{intercept_scale}{Optional override for the Intercept prior. If
\code{NULL}, uses the scale for the order-0 component (or 1.0 if absent).}
}
\value{
A brms prior table (the same kind that \code{brms::prior} returns).
}
\description{
For the order-0 component, sets \code{prior(normal(0, tau0), class = "Intercept")}.
For each higher-order component \verb{(d1, ..., dk)}, sets
\code{prior(normal(0, tau_alpha), class = "sd", group = "d1:...:dk")}.
}
\details{
Pass the returned object directly to \code{brms::brm(..., prior = ...)}.
}
