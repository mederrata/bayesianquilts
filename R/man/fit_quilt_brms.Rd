% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/brms_quilt.R
\name{fit_quilt_brms}
\alias{fit_quilt_brms}
\title{Fit a quilted brms model with manuscript-derived prior scales}
\usage{
fit_quilt_brms(
  response,
  data,
  interactions,
  predictors = NULL,
  noise_scale = 1,
  family = NULL,
  contingency_table = NULL,
  c = 0.5,
  per_component = FALSE,
  scales = NULL,
  ...
)
}
\arguments{
\item{response}{The response variable name (character).}

\item{data}{A data frame containing the response, predictors, and one
integer-coded (0..cardinality-1) column per dimension named in the
interactions.}

\item{interactions}{Either a \code{Decomposed}, an \code{Interactions}, or a list
passed to \code{Interactions$new(...)}.}

\item{predictors}{Optional character vector of additional fixed-effect
predictors (e.g. continuous covariates).}

\item{noise_scale}{Plug-in estimate of sigma (passed to the scale formula).}

\item{family}{A brms family (default \code{gaussian()}).}

\item{contingency_table}{Optional MultiwayContingencyTable to derive
per-cell sample sizes from the data instead of the uniform assumption.}

\item{c}{Effective-df budget (default 0.5).}

\item{per_component}{Use per-component bound (see Decomposed docs).}

\item{scales}{Optional named numeric of prior scales to skip the formula
derivation (e.g. for sensitivity analysis).}

\item{...}{Passed through to \code{brms::brm} (e.g. \code{chains}, \code{iter}, \code{seed}).}
}
\value{
A \code{brmsfit} object.
}
\description{
Convenience wrapper that:
\enumerate{
\item Builds a \code{Decomposed} from \code{interactions} (or accepts an existing one).
\item Computes \code{generalization_preserving_scales} (or uses caller-provided
scales).
\item Assembles the formula + prior list.
\item Calls \code{brms::brm(...)}.
}
}
