% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ensemble.R
\name{component_loo_elpd}
\alias{component_loo_elpd}
\title{Per-component LOO ELPD contributions}
\usage{
component_loo_elpd(fit, decomposed, sigma = NULL)
}
\arguments{
\item{fit}{A \code{brmsfit}.}

\item{decomposed}{A \code{Decomposed} object.}

\item{sigma}{Optional residual sd (Gaussian only). If NULL, uses the
posterior mean from \code{fit}.}
}
\value{
A list with:
\itemize{
\item \code{elpd} -- named numeric, per-component ELPD.
\item \code{loglik} -- list of (S, N) per-obs log-likelihood matrices, one per
component.
}
}
\description{
Computes, for each component alpha and each observation n, the
contribution to the model log-likelihood from setting all components
except alpha to zero. Aggregated across observations this gives a
per-component ELPD that can drive stacking weights.
}
\details{
Uses brms's posterior_linpred / log_lik scaffolding via
\code{component_predict}.
}
