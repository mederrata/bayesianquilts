% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/brms_lattice.R
\name{quilt_lattice_interactions}
\alias{quilt_lattice_interactions}
\title{Build an \code{Interactions} object from quantile-binned numeric features.}
\usage{
quilt_lattice_interactions(
  data,
  predictors,
  k = 4L,
  max_order = NULL,
  exclusions = NULL
)
}
\arguments{
\item{data}{Data frame containing the numeric \code{predictors} columns.}

\item{predictors}{Character vector of column names to bin.}

\item{k}{Either a single integer (same K for every feature) or a
named integer vector mapping \code{predictor -> K}.}

\item{max_order}{Maximum interaction order to retain (default: full
product, i.e. \code{length(predictors)}).}

\item{exclusions}{Optional list of character vectors, each naming a
subset of predictors that should be excluded from the decomposition
(passed through to \code{Interactions$new}).}
}
\value{
A list with components \code{interactions} (an \code{Interactions}
object), \code{binned} (a data frame with one binned-factor column per
predictor, named \verb{<predictor>} -- replacing the original numeric
columns), and \code{breaks} (a named list of the quantile break vectors
that produced each binning).
}
\description{
Build an \code{Interactions} object from quantile-binned numeric features.
}
