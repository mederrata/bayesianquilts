% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/brms_lattice.R
\name{fit_quilt_lattice_brms}
\alias{fit_quilt_lattice_brms}
\title{Fit a quilted lattice regression with brms.}
\usage{
fit_quilt_lattice_brms(
  response,
  data,
  predictors,
  k = 4L,
  max_order = NULL,
  exclusions = NULL,
  noise_scale = NULL,
  c = 0.5,
  per_component = TRUE,
  use_contingency_counts = TRUE,
  family = NULL,
  scales = NULL,
  ...
)
}
\arguments{
\item{response}{Character. Name of the outcome column in \code{data}.}

\item{data}{Data frame with \code{response} plus the continuous predictors
named in \code{predictors}.}

\item{predictors}{Character vector of continuous predictor names.}

\item{k}{Either a single integer (same K per predictor) or a named
integer vector. Number of quantile bins per predictor.}

\item{max_order}{Maximum interaction order to retain. Defaults to
\code{length(predictors)} (full product).}

\item{exclusions}{Optional list of character vectors of predictor
names to drop from the decomposition.}

\item{noise_scale}{Optional plug-in estimate of sigma. If \code{NULL},
defaults to \code{1.0} for non-gaussian families and to \code{sd(response)}
for \code{gaussian()}.}

\item{c}{Effective-df budget (default 0.5).}

\item{per_component}{If \code{TRUE}, use the per-component manuscript
bound (\code{tau ~ sigma / sqrt(p * N^alpha)}) rather than the per-
parameter bound.}

\item{use_contingency_counts}{If \code{TRUE}, derive per-cell sample sizes
from the actual data via a \code{MultiwayContingencyTable}; otherwise
the uniform-cells assumption is used (default \code{TRUE}).}

\item{family}{A brms family. Defaults to \code{brms::gaussian()}.}

\item{scales}{Optional named numeric vector of component prior sds to
override \code{generalization_preserving_scales}.}

\item{...}{Forwarded to \code{brms::brm} (e.g. \code{chains}, \code{iter}, \code{seed},
\code{refresh}).}
}
\value{
A list with \code{fit} (a \code{brmsfit}), \code{interactions} (the
\code{Interactions} object used), \code{binned} (binned data frame fed to
brms), \code{breaks} (per-predictor quantile breaks), and \code{scales} (the
tau_alpha values that drove the priors).
}
\description{
Quantile-bins the continuous \code{predictors}, builds the additive quilt
decomposition from the binned multi-index, computes
\code{generalization_preserving_scales} to set component prior sds, and
fits the resulting hierarchical model with \code{brms::brm}.
}
\details{
The fitted brmsfit has one group-level random intercept per
non-excluded interaction component; the overall fitted value at
covariate point \code{x} is the sum of those component contributions, which
is exactly the additive RG-lattice approximation in the manuscript.
}
