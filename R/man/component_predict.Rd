% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ensemble.R
\name{component_predict}
\alias{component_predict}
\title{Evaluate per-component contributions to the linear predictor}
\usage{
component_predict(fit, decomposed, newdata = NULL, interaction_sep = ":")
}
\arguments{
\item{fit}{A \code{brmsfit}.}

\item{decomposed}{A \code{Decomposed} object describing the interaction
structure (typically the one used to fit \code{fit}).}

\item{newdata}{Optional new data; defaults to the fitting data.}

\item{interaction_sep}{Separator used inside group-level terms.}
}
\value{
A named list of (S x N) matrices, one per non-excluded
component, each containing posterior draws of that component's
contribution to the linear predictor at the rows of \code{newdata}.
}
\description{
For a quilted brms fit produced by \code{fit_quilt_brms} (or any brmsfit
whose group-level structure matches a \code{Decomposed} object), returns
the per-component contributions to the linear predictor at each row
of \code{newdata}.
}
\details{
The order-0 component is the Intercept; order-k components are
group-level random intercepts. Continuous fixed-effect predictors,
if present, are reported as a "fixed" component.
}
