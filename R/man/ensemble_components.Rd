% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ensemble.R
\name{ensemble_components}
\alias{ensemble_components}
\title{Ensemble per-component contributions at the parameter level}
\usage{
ensemble_components(
  fit,
  decomposed,
  method = c("uniform", "aic", "stacking", "manual"),
  weights = NULL,
  newdata = NULL
)
}
\arguments{
\item{fit}{A \code{brmsfit}.}

\item{decomposed}{A \code{Decomposed} object.}

\item{method}{One of "uniform", "aic", "stacking", "manual".}

\item{weights}{If \code{method = "manual"}, a named numeric vector aligned
with the components.}

\item{newdata}{Optional \code{newdata} for predictions; defaults to fitting
data.}
}
\value{
A list with:
\itemize{
\item \code{weights} -- named numeric.
\item \code{contributions} -- list of (S, N) per-component matrices.
\item \code{ensemble} -- (S, N) matrix of ensembled linear-predictor draws.
\item \code{elpd} -- per-component ELPD (if method is \code{"aic"} or \code{"stacking"},
otherwise NULL).
}
}
\description{
Combines per-component contributions into an ensembled posterior of the
linear predictor:
\code{eta_ens[s, n] = sum_alpha w_alpha * contributions[[alpha]][s, n]}.
}
\details{
Weight schemes:
\itemize{
\item \code{"uniform"} -- w_alpha = 1/K.
\item \code{"aic"} -- w_alpha = exp(elpd_alpha - max) / sum(...); a softmax of
component ELPDs.
\item \code{"stacking"} -- optimal LOO stacking weights via \code{loo::stacking_weights}
on the per-observation per-component log-likelihoods. Requires the
\code{loo} package.
\item \code{"manual"} -- user-supplied numeric vector matching the components.
}

Note: with \code{"uniform"} or \code{"aic"}, the ensemble corresponds to a
weighted combination of additive submodels; for the additive
decomposition theta = sum_alpha theta_alpha, the natural choice for a
single coherent model is \code{w_alpha = 1} for all alpha, which is what a
fitted quilted brms gives you "for free" via \code{posterior_linpred}. The
ensembling here is useful when you want to \strong{reweight components by
predictive performance} -- e.g., to ablate low-ELPD high-order terms
before deployment.
}
